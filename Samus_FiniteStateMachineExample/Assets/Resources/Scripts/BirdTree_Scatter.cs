using UnityEngine;
using System.Collections;

public class BirdTree_Scatter : MonoBehaviour
{
    /*
    public static Vector3[] birdPositions; // Each Vector3 is a position on the groudn where a bird will reside.
    public static Vector3[] birdHoles; // Each Vector3 is a position where a Bird will fly.
    public GameObject birdType; // Must be a GameObject that used the Bird script.

    Bird[] birds = new Bird[birdHoles.Length];

    // Use this for initialization
    void Start()
    {

        for (int i = 0; i < birdHoles.Length; i++) { // Generates new birds, each taking one birdHole in birdHoles[]
            Bird bird = new Bird();
            bird.birdHole = birdHoles[i]; // Tell the newly created bird where to fly to.

            birds[i] = bird; // Adds newly created bird to birds array.
        }

        Shuffle(birds); // Shuffles the order of the birds.

        for (int j = 0; j < birds.Length; j++) {
            birds[j].birdHome = birdPositions[j];
        }

    }

    // Generic Fisher-Yates Shuffle Algorithm O(n). Shuffles in-place.
    private void Shuffle<T>(T[] array)
    {

        for (int i = array.Length; i > 1; i--)
        {
            // Pick random element to swap.
            int j = Random.Range(0, i); // 0 <= j <= i-1
            // Swap.
            T tmp = array[j];
            array[j] = array[i - 1];
            array[i - 1] = tmp;
        }
    }

     
     */
      
} // End class BirdTree_Scatter


